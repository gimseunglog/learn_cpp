#include <iostream>
using namespace std;

int main() {
    int A, B, C, D, E;
    cin >> A;
    cin >> B >> C >> D >> E;
    cout << (A > B) << endl;
    cout << (A > C) << endl;
    cout << (A > D) << endl;
    cout << (A > E) << endl;
    return 0;
}